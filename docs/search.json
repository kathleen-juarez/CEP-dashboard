[
  {
    "objectID": "Juarez-Kathleen_W10-Dashboard-Assgn\".html",
    "href": "Juarez-Kathleen_W10-Dashboard-Assgn\".html",
    "title": "Juarez-Kathleen_W10-Dashboard-Assgn",
    "section": "",
    "text": "https://kathleen-juarez.github.io/CEP-dashboard/CEP_dashboard.html\n#| label: load-packages and data \n#install.packages(\"tidyverse\")\nlibrary(tidyverse)\nlibrary(readr)\nlibrary(scales)\nlibrary(ggplot2)\nlibrary(dplyr)\n\n\n# load data\ndf &lt;- \n  read_csv(\"Traffic acquisition-Session primary channel group (Default Channel Group)(Sheet1)(2).csv\")\n\n# colnames(df)\n#| label: clean data\ndf_clean &lt;- \n  df %&gt;%\n  rename(channel = `...1`) %&gt;%\n  mutate(sessions = readr::parse_number(Sessions),\n        form_start = readr::parse_number(`Event count (form_start)`),\n        high_intent_rate = form_start / sessions)\ndf2 &lt;-\n  df_clean %&gt;%\n filter(!is.na(sessions)) %&gt;%\n  filter(sessions &gt;= 5) #controls for small sample sizes\n\n\n# visualization\nggplot(df2, aes(x = high_intent_rate, y = reorder(channel, high_intent_rate))) +\n  geom_col(aes(fill = channel == \"Direct\"), width = 0.7) +\n  geom_text(aes(label = percent(high_intent_rate, accuracy = 0.1)),\n            hjust = -0.1) +\n  scale_fill_manual(values = c(\"TRUE\" = \"#E69F00\", \"FALSE\" = \"grey70\"),\n                    guide = \"none\") +\n  scale_x_continuous(labels = percent_format(),\n                     expand = expansion(mult = c(0, .2))) +\n  labs(\n    title = \"Direct traffic leads in high-intent engagement\",\n    subtitle = \"Form starts per session\",\n    x = \"High-intent rate\",\n    y = NULL\n  ) +\n  theme_minimal()\n\n\n\ndf_plot &lt;- df_clean %&gt;%\n  filter(channel %in% c(\"Direct\", \"Organic Search\")) %&gt;%\n  mutate(high_intent_rate = form_start / sessions)\n\n\nggplot(df_plot, aes(x = channel, y = high_intent_rate, fill = channel == \"Direct\")) +\n  geom_col(width = 0.6) +\n  geom_text(\n    aes(label = percent(high_intent_rate, accuracy = 0.1)),\n    vjust = -0.5,\n    color = \"black\"\n  ) +\n  scale_fill_manual(\n    values = c(\"TRUE\" = \"#0072B2\", \"FALSE\" = \"grey70\"),\n    guide = \"none\"\n  ) +\n  scale_y_continuous(\n    labels = percent_format(),\n    limits = c(0, 0.08)\n  ) +\n  labs(\n    title = \"Direct traffic shows higher booking intent\",\n    subtitle = \"Difference is not statistically significant (p = 0.28)\",\n    x = NULL,\n    y = \"High-intent rate (form_start / sessions)\"\n  ) +\n  theme_minimal(base_size = 14) +\n  theme(\n    panel.grid.major = element_blank(),\n    panel.grid.minor = element_blank(),\n    axis.text = element_text(color = \"black\"),\n    axis.title = element_text(color = \"black\"),\n    plot.title = element_text(face = \"bold\")\n  )\n\n\n\n\n\n\ndf_plot &lt;- \n  df_clean %&gt;%\n  mutate(label = ifelse(sessions == 0 & high_intent_rate == 0, \"Other\", channel))\n\nggplot(df_clean, aes(x = sessions, y = high_intent_rate)) +\n  geom_point(size = 6, color = \"#0072B2\") +\n\ngeom_text(data = subset(df_plot, sessions &gt; 20),\n        aes(label = channel, nudge_x = ifelse(channel == \"Organic Social\", -40, 0)\n  ),\n  nudge_y = 0.004,\n  vjust = 0,\n  size = 6\n) +\n\n  scale_y_continuous(labels = scales::percent) +\n  labs(\n    x = \"Sessions\",\n    y = \"High-intent rate\"\n  ) +\n  theme_minimal() +\ntheme(\n  axis.title.x = element_text(size = 18),\n  axis.title.y = element_text(size = 18),\n  axis.text.x  = element_text(size = 14),\n  axis.text.y  = element_text(size = 14)\n)"
  },
  {
    "objectID": "Juarez-Kathleen_W10-Dashboard-Assgn\".html#direct-traffic-leads-in-high-intent-engagement",
    "href": "Juarez-Kathleen_W10-Dashboard-Assgn\".html#direct-traffic-leads-in-high-intent-engagement",
    "title": "Juarez-Kathleen_W10-Dashboard-Assgn",
    "section": "",
    "text": "# visualization\nggplot(df2, aes(x = high_intent_rate, y = reorder(channel, high_intent_rate))) +\n  geom_col(aes(fill = channel == \"Direct\"), width = 0.7) +\n  geom_text(aes(label = percent(high_intent_rate, accuracy = 0.1)),\n            hjust = -0.1) +\n  scale_fill_manual(values = c(\"TRUE\" = \"#E69F00\", \"FALSE\" = \"grey70\"),\n                    guide = \"none\") +\n  scale_x_continuous(labels = percent_format(),\n                     expand = expansion(mult = c(0, .2))) +\n  labs(\n    title = \"Direct traffic leads in high-intent engagement\",\n    subtitle = \"Form starts per session\",\n    x = \"High-intent rate\",\n    y = NULL\n  ) +\n  theme_minimal()\n\n\n\ndf_plot &lt;- df_clean %&gt;%\n  filter(channel %in% c(\"Direct\", \"Organic Search\")) %&gt;%\n  mutate(high_intent_rate = form_start / sessions)\n\n\nggplot(df_plot, aes(x = channel, y = high_intent_rate, fill = channel == \"Direct\")) +\n  geom_col(width = 0.6) +\n  geom_text(\n    aes(label = percent(high_intent_rate, accuracy = 0.1)),\n    vjust = -0.5,\n    color = \"black\"\n  ) +\n  scale_fill_manual(\n    values = c(\"TRUE\" = \"#0072B2\", \"FALSE\" = \"grey70\"),\n    guide = \"none\"\n  ) +\n  scale_y_continuous(\n    labels = percent_format(),\n    limits = c(0, 0.08)\n  ) +\n  labs(\n    title = \"Direct traffic shows higher booking intent\",\n    subtitle = \"Difference is not statistically significant (p = 0.28)\",\n    x = NULL,\n    y = \"High-intent rate (form_start / sessions)\"\n  ) +\n  theme_minimal(base_size = 14) +\n  theme(\n    panel.grid.major = element_blank(),\n    panel.grid.minor = element_blank(),\n    axis.text = element_text(color = \"black\"),\n    axis.title = element_text(color = \"black\"),\n    plot.title = element_text(face = \"bold\")\n  )"
  },
  {
    "objectID": "Juarez-Kathleen_W10-Dashboard-Assgn\".html#sessions-and-high-intent-rate",
    "href": "Juarez-Kathleen_W10-Dashboard-Assgn\".html#sessions-and-high-intent-rate",
    "title": "Juarez-Kathleen_W10-Dashboard-Assgn",
    "section": "",
    "text": "df_plot &lt;- \n  df_clean %&gt;%\n  mutate(label = ifelse(sessions == 0 & high_intent_rate == 0, \"Other\", channel))\n\nggplot(df_clean, aes(x = sessions, y = high_intent_rate)) +\n  geom_point(size = 6, color = \"#0072B2\") +\n\ngeom_text(data = subset(df_plot, sessions &gt; 20),\n        aes(label = channel, nudge_x = ifelse(channel == \"Organic Social\", -40, 0)\n  ),\n  nudge_y = 0.004,\n  vjust = 0,\n  size = 6\n) +\n\n  scale_y_continuous(labels = scales::percent) +\n  labs(\n    x = \"Sessions\",\n    y = \"High-intent rate\"\n  ) +\n  theme_minimal() +\ntheme(\n  axis.title.x = element_text(size = 18),\n  axis.title.y = element_text(size = 18),\n  axis.text.x  = element_text(size = 14),\n  axis.text.y  = element_text(size = 14)\n)"
  },
  {
    "objectID": "shiny-app.html",
    "href": "shiny-app.html",
    "title": "Interactive Shiny App",
    "section": "",
    "text": "https://kathleen-juarez.github.io/CEP-dashboard/shiny-app.html\n#install.packages(\"shiny\")\n#install.packages(\"shinydashboard\")\n#install.packages(\"plotly\")\n#install.packages(\"DT\")\n\nlibrary(\"tidyverse\")\nlibrary(\"shiny\")\nlibrary(\"shinydashboard\")\nlibrary(\"plotly\")\nlibrary(\"DT\")"
  },
  {
    "objectID": "shiny-app.html#httpskathleen-juarez.github.iocep-dashboardcep_dashboard.html",
    "href": "shiny-app.html#httpskathleen-juarez.github.iocep-dashboardcep_dashboard.html",
    "title": "Interactive Shiny App",
    "section": "",
    "text": "https://kathleen-juarez.github.io/CEP-dashboard/shiny-app.html\n#install.packages(\"shiny\")\n#install.packages(\"shinydashboard\")\n#install.packages(\"plotly\")\n#install.packages(\"DT\")\n\nlibrary(\"tidyverse\")\nlibrary(\"shiny\")\nlibrary(\"shinydashboard\")\nlibrary(\"plotly\")\nlibrary(\"DT\")"
  },
  {
    "objectID": "shiny-app.html#shiny-app-template",
    "href": "shiny-app.html#shiny-app-template",
    "title": "Interactive Shiny App",
    "section": "Shiny App Template",
    "text": "Shiny App Template\n\nlibrary(shiny)\n\nWarning: package 'shiny' was built under R version 4.5.2\n\n# Define UI for application that draws a histogram\n#ui &lt;- fluidPage(\n\n    \n# Define server logic required to draw a histogram\n#server &lt;- function(input, output) {\n  \n# Run the application \n#shinyApp(ui = ui, server = server)"
  },
  {
    "objectID": "shiny-app.html#page-layout-functions",
    "href": "shiny-app.html#page-layout-functions",
    "title": "Interactive Shiny App",
    "section": "Page layout functions",
    "text": "Page layout functions\nfluidPage()\nfixedPage()\nnavbarPage()\nfillPage()"
  }
]